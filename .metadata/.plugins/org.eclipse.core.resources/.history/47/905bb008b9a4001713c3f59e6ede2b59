fo = open("foo.txt", "r+b")
str = fo.read();
print (str)

# Check current position
position = fo.tell();
print ("Current file position : ", position)

# Reposition pointer at the beginning once again
position = fo.seek(100, 0);
str = fo.read();
print (str)
# Close opend file
fo.close()