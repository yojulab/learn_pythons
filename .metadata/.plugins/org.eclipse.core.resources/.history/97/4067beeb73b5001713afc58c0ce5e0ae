import sys
from PySide import QtCore, QtGui, QWidgets
class ButtonWindow(QWidgets.QWidget):
    def __init__(self, msg):
        QWidgets.QWidget.__init__(self)
        self.setWindowTitle("Click Window")
        self.setGeometry(300, 200, 200, 200)
        self.button = QWidgets.QPushButton(msg, self)
        self.button.setGeometry(50, 30, 100, 30)
        self.button.clicked.connect(self.hello)
    def hello(self):
        print ('Hello World')

if __name__ == "__main__" :
    app = QWidgets.QApplication(sys.argv)
    bw = ButtonWindow("Click me!")
    bw.show()
    sys.exit(app.exec_())